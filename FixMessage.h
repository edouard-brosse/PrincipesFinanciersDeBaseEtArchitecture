#ifndef FIXMESSAGE_H
#define FIXMESSAGE_H

#include <iostream>
#include <map>
#include <sstream>

class FixMessage
{
private:
    /* data */
public:
    FixMessage(/* args */);
    ~FixMessage();

    SpiltMsg(std::string msg);
};

#endif